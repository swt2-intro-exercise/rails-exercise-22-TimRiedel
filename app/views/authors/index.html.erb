<h1>All authors</h1>

<%= link_to 'Add new author', new_author_path%>

<table>
  <tr>
    <th>Name</th>
    <th>Homepage</th>
    <th></th>
  </tr>

  <% @authors.each do |author| %>
    <tr>
      <td><%= author.name %></td>
      <td><%= link_to author.homepage, 'https://' + author.homepage, target: :_blank%></td>
      <td><%= link_to 'Show', author_path(author) %></td>
    </tr>
  <% end %>
</table>
